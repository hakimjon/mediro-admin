name: mediro_admin
description: "Mediro Admin Panel — web-only admin dashboard for the Usta-Market platform."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3
  get: ^4.6.6
  get_storage: ^2.1.1
  supabase_flutter: ^2.10.1
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  # Photo upload for provider avatars + equipment galleries (web-compatible).
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
